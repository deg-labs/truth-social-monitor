services:
  truth-social-monitor:
    build: .
    env_file:
      - .env
    volumes:
      - truth-social-cache:/app/cache
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

volumes:
  truth-social-cache:
